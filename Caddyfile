# Caddy gets and renews the HTTPS certificate for your domain automatically.
{$DOMAIN} {
	encode gzip zstd
	request_body {
		max_size 110MB
	}
	reverse_proxy app:3000
}

www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
